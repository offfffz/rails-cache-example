version: '2'

services:
  web:
    build: .
    ports:
      - 3000:3000
    volumes:
      - .:/var/app
    depends_on:
      - db
    environment:
      - DB_USERNAME=postgres
      - DB_HOST=db
    command: bundle exec rails s -p 3000 -b 0.0.0.0
  db:
    image: postgres
